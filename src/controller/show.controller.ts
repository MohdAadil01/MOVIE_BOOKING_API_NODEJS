// get a show
// get all shows
// update show
// delete a show
// add a show

import asyncHandler from "../utils/asyncHandler";

export const addShow = asyncHandler(async () => {});

export const updateShow = asyncHandler(async () => {});

export const getShow = asyncHandler(async () => {});

export const getShows = asyncHandler(async () => {});

export const deleteShow = asyncHandler(async () => {});
